Action Path Android App
=======================

This is the Android client for (ActionPath)[http://actionpath.org].

Installation
------------

This is build using AndroidStudio (v1.3 or greater).

Releasing
---------

Change the version number and string in `app/build.gradle`.

